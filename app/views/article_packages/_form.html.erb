<%= form_for @picture, multipart: true, html: {class: :dropzone, id: 'picture-dropzone'} do |p| %>
    <%= p.hidden_field :code %>    
<% end %>


<%= nested_form_for @article_package, :html => { :class => 'form-horizontal', multipart: true } do |f| %>



<hr>


  <div class="control-group">
    <%= f.label "Article Package Number", :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :article_package_number, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :title, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :title, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :description, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :description, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :price, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :price, :class => 'text_field' %>
    </div>
  </div>  

  <h5>datepicker: Widget given by jQuery UI</h5>
  <p>I installed jQuery UI via 1) adding <strong>gem 'jquery-ui-rails' </strong> and 2) adding <strong>//= require jquery-ui </strong> in application.js</p>
  <div class="datepicker"></div>

  <div class="control-group">
    <%= f.label :pictures, :class => 'control-label' %>
    <div class="controls">
      <p>
<!--         <div id="dropzone">   
          <h5>Drag files here</h5>
        </div> -->
<!--  <h5>Drag files here</h5>
 


<hr>
        <% if @article_package.pictures.length!=0 %>
          <% @article_package.pictures.each do |picture| %>
            <p>
              <%= image_tag picture.image.url(:thumb)%>
              <%= picture.image.path(:thumb) %>
            </p>

            <!-- I want to be able to delete selected images from the article package
                             !!! does NOT work yet -->
            <!-- Delete image -->

          <% end %>          
        <% end %>
<hr>
        <%= file_field_tag "images[]", type: :file, multiple: true, id:"fileselect" %>
        <h5>Uploaded images</h5>
        <output id="list"></output>
      </p>

    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, id:"submitbutton", :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                article_packages_path, :class => 'btn btn-mini' %>
  </div>
<% end %>